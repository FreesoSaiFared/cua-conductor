interface:
  display_name: "Cua Conductor"
  short_description: "Repo-specific patterns and workflows for cua-conductor"
  default_prompt: "Use the cua-conductor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true